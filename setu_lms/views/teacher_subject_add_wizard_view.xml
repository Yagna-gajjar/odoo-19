<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="teacher_subject_add_wizard_form_view" model="ir.ui.view">
        <field name="name">teacher.subject.add.wizard.form.view</field>
        <field name="model">teacher.subject.add.wizard</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="teacher_id" readonly="1"/>
                    <field name="class_subject_ids" widget="many2many_tags"/>
                </group>
                <footer>
                    <button name="action_add_class_subjects" type="object" string="Add Class Subjects" class="btn-primary"/>
                    <button string="Cancel" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="teacher_subject_add_wizard_action" model="ir.actions.act_window">
        <field name="name">Add Class Subjects</field>
        <field name="res_model">teacher.subject.add.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>